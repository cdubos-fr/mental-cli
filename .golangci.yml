version: "2"
linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - gocritic
    - revive
    - unconvert
    - unparam
  # misspell is disabled: the command output/messages are intentionally French.
formatters:
  enable:
    - gofmt
    - goimports
